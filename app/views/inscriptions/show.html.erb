<div id="s-i-head">
  <div class="container">
    <div id="sih-link">
    <%= link_to "Retour", root_path %>
    </div>

    <div id="sih-info">
    <h1><%= @inscription.fidelity_program.name %></h1>
    <p><%= @inscription.fidelity_program.user.business_name %></p>
    </div>
  </div>
</div>

<div id="s-i-body" class="container">
  <div id="sib-title">
    <p>Mes points<p>
  </div>
  <div id="sib-points">
    <div id="sib-number">
      <% @inscription.participations.each do |participation| %>
      <% @sum += participation.points %>
      <% end %>
      <%= @sum %>
    </div>
  </div>
</div>

<div id="s-i-btns" class="container">
  <div id="si-rewards">
    <%= link_to "Mes récompenses", customers_rewards_path, class: "td-none" %>
  </div>
  <div id="si-history">
    <%= link_to "Historique", history_path(@fidelity_program, @inscription), class: "td-none" %>
  </div>
</div>

<%= link_to "Mes programmes", root_path %>

<%= simple_form_for [@fidelity_program, @inscription, @participation], enctype:"multipart/form-data"  do |f| %>
  <!-- [...] -->
  <%= f.input :photo, as: :file, input_html:{class:"hidden-input", id:"photo-upload"} %>
  <label for="photo-upload">
    <div class="tabler--line-scan"></div>
  </label>
  <%= f.submit %>
<% end %>
